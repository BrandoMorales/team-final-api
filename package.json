{
  "name": "team-final-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prestart": "npm install -g json-server",
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": "18.x"
  }
}
